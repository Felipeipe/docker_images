# NavDP

## Para correr
ejecutar:

```bash
docker compose up -d --build --remove-orphans
```

Si haces algún cambio en el `docker-compose.yml` (sin hacer cambios en el dockerfile)

```bash
docker compose up -d --remove-orphans
```
